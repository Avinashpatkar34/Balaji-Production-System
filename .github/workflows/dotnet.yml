name: .NET

on:
  on the runner.
2. Verify that the `Microsoft.WebApplication.targets` file exists at the specified location push:
    branches:
      - main
  pull_request:
.

Alternatively, update the project file to use a valid path or SDK version.

Example:

```xml    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .
<Project Sdk="Microsoft.NET.Sdk.Web">
  ...
  <Import Project="$(MSBuildSDKsPath)/Microsoft.WebApplication.targetsNET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '8.0.x' # Ensure this is" />
  ...
</Project>
    - name: Test
      run: dotnet test --no-build --verbosity normal
